# CC-Middleware Progress

## Status Legend
- [ ] Not started
- [~] In progress
- [x] Completed (verified)
- [!] Failed verification (needs fix)

---

## Phase 1: Foundation
- [x] Task 1.1: Initialize project scaffold
- [x] Task 1.2: Define core types
- [x] Task 1.3: Create test harness

## Phase 2: Session Discovery
- [x] Task 2.1: Session discovery from filesystem
- [x] Task 2.2: Session message reading
- [x] Task 2.3: Session info and metadata

## Phase 3: Session Launching
- [x] Task 3.1: Launch headless session (single-turn)
- [x] Task 3.2: Launch streaming session
- [x] Task 3.3: Resume and continue sessions
- [x] Task 3.4: Session lifecycle management

## Phase 4: Event System
- [x] Task 4.1: Event bus core
- [x] Task 4.2: Blocking hook stubs
- [x] Task 4.3: SDK hook integration
- [x] Task 4.4: HTTP hook server

## Phase 5: Permission Handling
- [x] Task 5.1: Permission policy engine
- [x] Task 5.2: canUseTool implementation
- [x] Task 5.3: AskUserQuestion handling

## Phase 6: Agent & Team Management
- [x] Task 6.1: Agent definition reader
- [x] Task 6.2: Agent definition registry
- [x] Task 6.3: Team management
- [x] Task 6.4: Programmatic agent launching

## Phase 6.5: Integration Tests (PRIORITY)
- [x] Task 6.5.1: Hooks + Session integration
- [x] Task 6.5.2: Permissions + Session integration
- [x] Task 6.5.3: HTTP hook server + Claude CLI integration
- [x] Task 6.5.4: Session rename/tag roundtrip
- [x] Task 6.5.5: Streaming abort
- [x] Task 6.5.6: Full middleware smoke test

## Phase 7: API Layer
- [x] Task 7.1: Fastify server setup
- [x] Task 7.2: Session REST endpoints
- [x] Task 7.3: WebSocket streaming
- [x] Task 7.4: Hook and event endpoints
- [x] Task 7.5: Agent endpoints
- [x] Task 7.6: Permission endpoints

## Phase 8: Plugin Integration
- [x] Task 8.1: Plugin manifest
- [x] Task 8.2: Plugin hooks configuration
- [x] Task 8.3: Plugin skill
- [x] Task 8.4: Plugin MCP server (optional)

## Phase 9: Search & Indexing
- [x] Task 9.1: SQLite session store
- [x] Task 9.2: Session indexer
- [x] Task 9.3: Full-text search
- [x] Task 9.4: Search API endpoints

## Phase 10: Configuration Management
- [x] Task 10.1: Settings reader
- [x] Task 10.2: Settings writer
- [x] Task 10.3: Plugin reader and management
- [x] Task 10.4: Skills, agents, and rules reader
- [x] Task 10.5: MCP server reader
- [x] Task 10.6: Memory reader
- [x] Task 10.7: Configuration API endpoints

## Phase 11: CLI Control Surface
- [x] Task 11.1: CLI scaffold and client
- [x] Task 11.2: Server commands
- [x] Task 11.3: Session commands
- [x] Task 11.4: Hook commands
- [x] Task 11.5: Agent and team commands
- [x] Task 11.6: Permission commands
- [x] Task 11.7: Configuration commands
- [x] Task 11.8: Tab completion and polish
- [x] Task 11.9: Integration tests

## Phase 12: Real-Time Sync
- [x] Task 12.1: Session file watcher
- [x] Task 12.2: Config and component watcher
- [x] Task 12.3: Incremental auto-indexer
- [x] Task 12.4: WebSocket push for external changes
- [x] Task 12.5: Wire into server startup
- [x] Task 12.6: Watcher configuration

## Phase 13: Analytics & Developer Insights
- [x] Task 13.1: Analytics warehouse foundation
- [x] Task 13.2: Transcript backfill importer
- [x] Task 13.3: Keyword taxonomy and matching engine
- [x] Task 13.4: Live capture hardening
- [x] Task 13.5: Derived facts and rollups
- [x] Task 13.6: Analytics API
- [x] Task 13.7: Playground analytics view
- [x] Task 13.8: Optional OTel enrichment
- [x] Task 13.9: Fixtures, verification, and documentation

## Post-Plan Feature Work
- [x] Dispatch subsystem slice 1: hook parity, launch parity, durable queue store, executor/worker, dispatch API, cue bridge, cron scheduling, and heartbeat materialization

---

## Completion Log

| Date | Task | Status | Implemented By | Verified By | Notes |
|------|------|--------|----------------|-------------|-------|
| 2026-04-08 | Playground/session detail chat: suppress hyphenated low-signal transcript events like `last-prompt` | Passed | Orchestrator | Orchestrator | Fixed the remaining repeated metadata ghosts in the session chat by canonicalizing transcript event keys before low-signal filtering, so raw transcript types like `last-prompt` and `queue-operation` are dropped the same way as underscore-style variants; added regression coverage and verified with `npx vitest run tests/unit/session-detail-normalize.test.ts tests/unit/session-chat-turns.test.ts`, `npm run build`, and `npm run playground:build`. |
| 2026-04-08 | Playground/session detail chat: remove duplicated assistant replies and composer success echo | Passed | Orchestrator | Orchestrator | Stopped the session composer from echoing successful assistant output below the input area, and changed assistant-turn rendering so pure reply turns render once without a duplicate preview header or extra `Reply` label while activity-backed turns only show a muted summary when there is no final response; verified with `npx vitest run tests/unit/session-chat-turns.test.ts tests/unit/session-detail-normalize.test.ts`, `npm run build`, and `npm run playground:build`. |
| 2026-04-08 | Playground/session detail chat: remove redundant transcript labels and generic markdown headers | Passed | Orchestrator | Orchestrator | Pruned low-signal system transcript noise like bare `Tag`/`Queue Operation` echoes during session-detail normalization, suppressed generic commentary placeholders such as `Assistant`, made activity rows content-first so labels only appear when they add information, and removed the fake `Markdown block` header row from fenced markdown code blocks so replies read like chat instead of component diagnostics; verified with `npx vitest run tests/unit/session-detail-normalize.test.ts`, `npm run build`, and `npm run playground:build`. |
| 2026-04-08 | Playground/session detail chat: remove card-heavy transcript chrome and align closer to Craft chat structure | Passed | Orchestrator | Orchestrator | Flattened the session chat from stacked assistant/system cards into a denser divider-led transcript with compact assistant turn summaries, collapsible activity sections, simpler inline metadata, lighter user bubbles, and a less panelized chat shell so the sidebar behaves more like Craft's grouped chat reader than a stack of inspector boxes; verified with `npm run build` and `npm run playground:build`. |
| 2026-04-08 | Playground/session detail chat: Craft-style turn cards and activity mapping | Passed | Orchestrator | Orchestrator | Studied Craft Agents' session UI and message grouping (`/tmp/craft-agents-oss/packages/ui/src/components/chat/SessionViewer.tsx`, `TurnCard.tsx`, `turn-utils.ts`, plus renderer event handlers) and adapted the same user/system/assistant-turn grammar into the playground session chat. Added `playground/src/lib/session-chat-turns.ts`, rewrote the session detail transcript UI around assistant turn cards with ordered activity rows and final responses, added focused grouping tests, and verified with `npx vitest run tests/unit/session-chat-turns.test.ts tests/unit/session-detail-normalize.test.ts`, `npm run build`, and `npm run playground:build`. |
| 2026-04-08 | Playground/session launch: ignore synthetic detail models on resume/restart | Passed | Orchestrator | Orchestrator | Hardened `buildSDKOptions()` to drop placeholder model identifiers like `<synthetic>` or `synthetic` before any SDK launch path uses them, stopped the session detail composer from posting display-only models, and verified with `npm run build`, `npx vitest run tests/unit/session-utils.test.ts`, and `npm run playground:build`. |
| 2026-04-08 | Dispatch subsystem slice 1.1 multi-session correctness | Passed | Orchestrator | Orchestrator | Added session-aware schedule and heartbeat rule targeting, default session-based concurrency keys for resume/fork jobs, guarded job claiming so same-session work cannot be leased twice at once, parallel worker execution for distinct sessions, and regression coverage proving same-session serialization plus multi-session concurrency; verified with `npx tsc --noEmit` and `npx vitest run tests/unit/dispatch-store.test.ts tests/unit/dispatch-executor.test.ts tests/unit/dispatch-cues.test.ts tests/unit/dispatch-scheduler.test.ts tests/unit/dispatch-worker.test.ts tests/e2e/api-dispatch.test.ts tests/unit/event-bus.test.ts tests/unit/sdk-bridge.test.ts tests/e2e/hook-server.test.ts tests/e2e/api-sessions.test.ts`. |
| 2026-04-08 | Dispatch subsystem slice 1 | Passed | Tesla + Banach + Epicurus + Orchestrator | Beauvoir | Added `src/dispatch/` with durable jobs/runs/cues/schedules/heartbeat rules, queue executor/worker, hook-triggered cue bridge, timezone-aware cron next-run calculation, heartbeat materialization, `/api/v1/dispatch/*` routes, startup wiring in `main.ts`, public exports, and launch-path parity for main-thread `agent` plus middleware-installed SDK hooks on API/WebSocket launches; verified with `npx tsc --noEmit`, `npx vitest run tests/unit/dispatch-store.test.ts tests/unit/dispatch-executor.test.ts tests/unit/dispatch-cues.test.ts tests/unit/dispatch-scheduler.test.ts tests/e2e/api-dispatch.test.ts tests/e2e/api-sessions.test.ts`, and the hook parity suite (`tests/unit/event-bus.test.ts tests/unit/sdk-bridge.test.ts tests/e2e/hook-server.test.ts`). |
| 2026-04-04 | Task 1.1 | Passed | Orchestrator | Orchestrator | package.json, tsconfig.json, vitest.config.ts, .gitignore created; all deps installed; tsc --noEmit passes |
| 2026-04-04 | Task 1.2 | Passed | Orchestrator | Orchestrator | sessions.ts, hooks.ts, agents.ts, errors.ts, index.ts created; all types compile cleanly |
| 2026-04-04 | Task 1.3 | Passed | Orchestrator | Orchestrator | Test harness with fixtures, setup helpers, unit tests (13), E2E tests (4); all 17 tests pass |
| 2026-04-04 | Task 2.1 | Passed | Orchestrator | Orchestrator | discovery.ts wraps listSessions(); 7 E2E tests pass against real filesystem |
| 2026-04-04 | Task 2.2 | Passed | Orchestrator | Orchestrator | messages.ts wraps getSessionMessages(); text/tool extraction helpers; 4 E2E tests pass |
| 2026-04-04 | Task 2.3 | Passed | Orchestrator | Orchestrator | info.ts wraps getSessionInfo/renameSession/tagSession; 3 E2E tests pass |
| 2026-04-04 | Task 4.1 | Passed | Orchestrator | Orchestrator | event-bus.ts with typed EventEmitter, wildcard support, dispatch; 11 unit tests pass |
| 2026-04-04 | Task 4.2 | Passed | Orchestrator | Orchestrator | blocking.ts with default stubs, matcher support, register/unregister; 9 unit tests pass |
| 2026-04-04 | Task 4.3 | Passed | Orchestrator | Orchestrator | sdk-bridge.ts bridges event bus + blocking registry to SDK hooks format; 8 unit tests pass |
| 2026-04-04 | Task 4.4 | Passed | Orchestrator | Orchestrator | server.ts HTTP hook server with Fastify; always 200, JSON body for decisions; 7 E2E tests pass |
| 2026-04-04 | Task 3.1 | Passed | Orchestrator | Orchestrator | launcher.ts wraps query(); launchSession/resumeSession/continueSession/forkSession; 1 E2E test passes (real API call) |
| 2026-04-04 | Task 3.2 | Passed | Orchestrator | Orchestrator | streaming.ts with SessionStreamEvent types, event normalization, abort control; 1 E2E test passes |
| 2026-04-04 | Task 3.3 | Passed | Orchestrator | Orchestrator | Resume via resumeSession(); E2E test verifies context preservation across sessions |
| 2026-04-04 | Task 3.4 | Passed | Orchestrator | Orchestrator | manager.ts with SessionManager tracking, lifecycle events, abort; 2 E2E tests pass |
| 2026-04-04 | Task 5.1 | Passed | Orchestrator | Orchestrator | policy.ts with PolicyEngine, glob/alternation patterns, Bash(pattern) conditions, priority ordering; 11 unit tests pass |
| 2026-04-04 | Task 5.2 | Passed | Orchestrator | Orchestrator | handler.ts with createCanUseTool, PermissionManager, pending resolution, timeout; 6 unit tests pass |
| 2026-04-04 | Task 5.3 | Passed | Orchestrator | Orchestrator | ask-user.ts with AskUserQuestionManager, handler registration, default answers, pending questions; 6 unit tests pass |
| 2026-04-04 | Task 6.1 | Passed | Orchestrator | Orchestrator | definitions.ts reads agent markdown with gray-matter, parses frontmatter; 7 unit tests pass |
| 2026-04-04 | Task 6.2 | Passed | Orchestrator | Orchestrator | registry.ts with AgentRegistry, filesystem loading, runtime override, SDK conversion; 8 unit tests pass |
| 2026-04-04 | Task 6.3 | Passed | Orchestrator | Orchestrator | teams.ts with TeamManager, team discovery, task reading from filesystem; 6 unit tests pass |
| 2026-04-04 | Task 6.4 | Passed | Orchestrator | Orchestrator | launcher.ts with AgentLauncher using registry + session manager; 2 tests pass (1 real API call) |
| 2026-04-04 | Task 6.5.1 | Passed | Orchestrator | Orchestrator | hooks-session.test.ts: PreToolUse/PostToolUse events fire during real session; blocking deny prevents Bash execution; 2 tests pass |
| 2026-04-04 | Task 6.5.2 | Passed | Orchestrator | Orchestrator | permissions-session.test.ts: PolicyEngine deny-bash/allow-read verified with real API; Read succeeds, result contains project name; 1 test passes |
| 2026-04-04 | Task 6.5.3 | Passed | Orchestrator | Orchestrator | hookserver-cli.test.ts: HTTP hook server receives PostToolUse from real claude -p session via --settings; 1 test passes |
| 2026-04-04 | Task 6.5.4 | Passed | Orchestrator | Orchestrator | session-metadata.test.ts: launch, rename, tag, read-back, clear-tag roundtrip works; 1 test passes |
| 2026-04-04 | Task 6.5.5 | Passed | Orchestrator | Orchestrator | streaming-abort.test.ts: streaming session receives events then aborts mid-stream; AbortController verified; 1 test passes |
| 2026-04-04 | Task 6.5.6 | Passed | Orchestrator | Orchestrator | smoke.test.ts: full stack (hooks+permissions+agents+session) wired together; result correct ("4"); hook events fire; agent registry works; 1 test passes |
| 2026-04-04 | Task 7.1 | Passed | Orchestrator | Orchestrator | server.ts: Fastify API server with health check, status endpoint, error handling, 404 handler, context decoration; 3 E2E tests pass |
| 2026-04-04 | Task 7.2 | Passed | Orchestrator | Orchestrator | routes/sessions.ts: List, get, messages, launch, resume, abort, update endpoints with Zod validation; 9 E2E tests pass |
| 2026-04-04 | Task 7.3 | Passed | Orchestrator | Orchestrator | websocket.ts: WS endpoint with subscribe/unsubscribe, ping/pong, hook event broadcast, session lifecycle broadcast; 6 E2E tests pass |
| 2026-04-04 | Task 7.4 | Passed | Orchestrator | Orchestrator | routes/events.ts: event type listing, webhook subscription CRUD, webhook delivery with event filtering; 8 E2E tests pass |
| 2026-04-04 | Task 7.5 | Passed | Orchestrator | Orchestrator | routes/agents.ts: agent CRUD (list, get, register, delete) + team endpoints (list, get, tasks); 8 E2E tests pass |
| 2026-04-04 | Task 7.6 | Passed | Orchestrator | Orchestrator | routes/permissions.ts: policy CRUD, pending permission resolution, question answering, Zod validation; 10 E2E tests pass |
| 2026-04-04 | Task 8.1 | Passed | Orchestrator | Orchestrator | plugin.json manifest, settings.json with env vars; loads cleanly with claude --plugin-dir |
| 2026-04-04 | Task 8.2 | Passed | Orchestrator | Orchestrator | hooks.json with HTTP hooks for 9 lifecycle events; wildcard matchers for tool events; 3 E2E tests pass |
| 2026-04-04 | Task 8.3 | Passed | Orchestrator | Orchestrator | SKILL.md with frontmatter, 8 API endpoint references, $ARGUMENTS support; 2 E2E tests pass |
| 2026-04-04 | Task 8.4 | Passed | Orchestrator | Orchestrator | MCP server with createSdkMcpServer, 4 tools (sessions, status, search, agents); 1 unit test passes |
| 2026-04-04 | Task 9.1 | Passed | Orchestrator | Orchestrator | SQLite store with FTS5, triggers, WAL mode, prepared statements; 19 unit tests pass |
| 2026-04-04 | Task 9.2 | Passed | Orchestrator | Orchestrator | SessionIndexer with full/incremental modes, message extraction, stats; 4 E2E tests pass |
| 2026-04-04 | Task 9.3 | Passed | Orchestrator | Orchestrator | FTS5 search with relevance scoring, highlights, filters, pagination; 14 unit tests pass |
| 2026-04-04 | Task 9.4 | Passed | Orchestrator | Orchestrator | Search API (GET /search, POST /reindex, GET /stats); server updated with store/indexer; 7 E2E tests pass |
| 2026-04-04 | Task 10.1 | Passed | Orchestrator | Orchestrator | Settings reader for all scopes, merge with precedence, provenance tracking; 11 unit tests pass |
| 2026-04-04 | Task 10.2 | Passed | Orchestrator | Orchestrator | Atomic settings writer, permission rule helpers, dot-notation keys; 13 unit tests pass |
| 2026-04-04 | Task 10.3 | Passed | Orchestrator | Orchestrator | Plugin reader from installed_plugins.json, manifest parsing, enable/disable; 4 E2E tests pass |
| 2026-04-04 | Task 10.4 | Passed | Orchestrator | Orchestrator | Skills/agents/rules/CLAUDE.md discovery with gray-matter, agent CRUD; 8 E2E tests pass |
| 2026-04-04 | Task 10.5 | Passed | Orchestrator | Orchestrator | MCP server discovery from ~/.claude.json, .mcp.json, managed paths; 3 E2E tests pass |
| 2026-04-04 | Task 10.6 | Passed | Orchestrator | Orchestrator | Memory reader with project key encoding, index/topic files; 5 E2E tests pass |
| 2026-04-04 | Task 10.7 | Passed | Orchestrator | Orchestrator | 30+ config API routes for settings, plugins, agents, skills, rules, MCP, memory, CLAUDE.md; 21 E2E tests pass |
| 2026-04-04 | Task 11.1 | Passed | Orchestrator | Orchestrator | CLI scaffold with commander, client.ts (HTTP), ws-client.ts (WebSocket), output.ts (tables/JSON), auto-start.ts; ccm --help works |
| 2026-04-04 | Task 11.2 | Passed | Orchestrator | Orchestrator | server start/stop/status commands with PID file management, background/foreground modes; tested with live server |
| 2026-04-04 | Task 11.3 | Passed | Orchestrator | Orchestrator | sessions list/show/launch/resume/stream/search with table output, --json flag, WebSocket streaming, spinner |
| 2026-04-04 | Task 11.4 | Passed | Orchestrator | Orchestrator | hooks listen (WebSocket live stream with Ctrl+C) and hooks list (event types + subscriptions) |
| 2026-04-04 | Task 11.5 | Passed | Orchestrator | Orchestrator | agents list/show/create + teams list/show; table output with source, model, description |
| 2026-04-04 | Task 11.6 | Passed | Orchestrator | Orchestrator | permissions list/add/pending/approve/deny; matched to actual API schema (rules, behavior, toolName) |
| 2026-04-04 | Task 11.7 | Passed | Orchestrator | Orchestrator | config show/get/set/plugins/mcp/skills/agents/memory; scope-colored output, flattenObject for dot-notation |
| 2026-04-04 | Task 11.8 | Passed | Orchestrator | Orchestrator | Shell completion for bash/zsh/fish; graceful error handling (no stack traces); 13 polish E2E tests pass |
| 2026-04-04 | Task 11.9 | Passed | Orchestrator | Orchestrator | 15 integration tests against live server; full session/config/permissions/agents/hooks workflows; 311 total tests pass |
| 2026-04-04 | Task 12.1 | Passed | Orchestrator | Orchestrator | SessionWatcher with chokidar + polling hybrid, debounce, discover/update/remove events; 10 unit tests pass |
| 2026-04-04 | Task 12.2 | Passed | Orchestrator | Orchestrator | ConfigWatcher for settings, MCP, agents, skills, rules, teams, plugins, memory; 8 unit tests pass |
| 2026-04-04 | Task 12.3 | Passed | Orchestrator | Orchestrator | AutoIndexer with immediate index for discovered, batched index for updated; 7 unit tests pass |
| 2026-04-04 | Task 12.4 | Passed | Orchestrator | Orchestrator | WebSocket sync event types, WebSocketBroadcaster interface; 15 unit tests + 6 E2E tests pass |
| 2026-04-04 | Task 12.5 | Passed | Orchestrator | Orchestrator | Watchers wired into main.ts startup, /api/v1/sync/status endpoint, clean shutdown; 4 E2E tests pass |
| 2026-04-04 | Task 12.6 | Passed | Orchestrator | Orchestrator | Env var config, ccm sync status/reindex CLI commands, sync route module, public exports; 9 unit tests pass; 364 total tests pass |
| 2026-04-06 | Docs: OpenAPI/AsyncAPI spec compliance | Passed | Orchestrator | Orchestrator | Fixed OpenAPI 3.1 schema issues (`nullable` -> union types, root public security, SPDX license identifier), aligned AsyncAPI and docs with the actual WebSocket protocol, updated Mint config/script wiring, and verified with `npm run build`, `npm run validate:openapi`, `npm run validate:asyncapi`, and `npm run docs:build` |
| 2026-04-06 | CLI/WebSocket session streaming alignment | Passed | Orchestrator | Orchestrator | Implemented WebSocket `launch`/`resume` handling plus `session:stream` server events, fixed CLI streaming race/subscription issues, aligned AsyncAPI and session docs, and verified with `npm run build`, `npx vitest run tests/unit/ws-sync-events.test.ts tests/e2e/api-websocket.test.ts tests/e2e/cli-integration.test.ts tests/e2e/cli-streaming.test.ts`, `npm run validate:openapi`, `npm run validate:asyncapi`, and `npm run docs:build` |
| 2026-04-06 | Docs: early-alpha release cleanup | Passed | Orchestrator | Orchestrator | Added shared 4XX responses to every OpenAPI operation that was missing one, updated README and docs to clearly label `v0.1.0` as early alpha, stacked the SDK/Plugin/Hybrid mode section vertically, and added alpha-status notes in the API docs; verified with `npm run build`, `npm run validate:openapi`, `npm run validate:asyncapi`, and `npm run docs:build` |
| 2026-04-06 | Docs: convert diagram-like code snippets to Mermaid | Passed | Orchestrator | Orchestrator | Replaced diagram-like ASCII/code snippets in published docs and repo docs with Mermaid-backed diagrams, generated Mintlify SVG assets for the docs-site pages, and verified with `npm run docs:build` |
| 2026-04-06 | Config: Claude resource inventory exposure | Passed | Orchestrator | Orchestrator | Added plugin-aware discovery for skills, legacy commands, and agents; enriched installed plugin metadata with marketplace/blocklist/component counts; exposed `GET /api/v1/config/commands` and `GET /api/v1/config/runtime`; added `ccm config commands`; and verified with `npm run build` plus `npx vitest run tests/e2e/config-components.test.ts tests/e2e/api-config.test.ts tests/e2e/config-plugins.test.ts` |
| 2026-04-06 | Config: Claude global state + marketplace exposure | Passed | Orchestrator | Orchestrator | Added sanitized `~/.claude.json` readers, tracked-project lookup, known marketplace and marketplace-plugin inventory, new config API routes (`/global`, `/projects`, `/marketplaces`), CLI commands (`global`, `projects`, `marketplaces`, `marketplace-plugins`, `runtime`), deterministic fake-home E2E fixtures, and verified with `npm run build` plus `npx vitest run tests/e2e/api-config.test.ts tests/e2e/config-plugins.test.ts tests/e2e/config-components.test.ts` |
| 2026-04-06 | Research: Claude settings and plugin management contract | Passed | Orchestrator | Orchestrator | Cross-checked Anthropic's settings/plugin/SDK docs with local `~/.claude` state and Claude CLI behavior, documented which surfaces are declarative settings vs operational registries vs live runtime state, and updated research docs for `~/.claude.json` and the plugin system with explicit middleware exposure/management guidance |
| 2026-04-06 | Config: Claude CLI-backed management verification | Passed | Orchestrator | Orchestrator | Finished converting config API E2E tests to Fastify `app.inject()` so they run in-process under sandbox, added fake-`claude` usage coverage for plugin install/update/uninstall and marketplace add/update/remove, and re-verified with `npm run build` plus `npx vitest run tests/e2e/api-config.test.ts tests/e2e/config-plugins.test.ts tests/e2e/config-components.test.ts` |
| 2026-04-06 | Config: global preference management + plugin provenance | Passed | Orchestrator | Orchestrator | Added guarded writes for documented `~/.claude.json` preferences, exposed plugin provenance that joins enablement-by-scope, marketplace/catalog presence, and runtime-loaded state, extended CLI/completions, and verified with `npm run build` plus `npx vitest run tests/e2e/api-config.test.ts tests/e2e/config-plugins.test.ts tests/e2e/config-components.test.ts` |
| 2026-04-07 | Playground: shadcn local demo dashboard | Passed | Orchestrator | Orchestrator | Added a Vite + React playground under `playground/` using shadcn-style UI primitives and a Mintlify-inspired layout, with live proofs for search, session stream events, hook/team feed visibility, agents, teams, and runtime inventory; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: linear UI cleanup + search-index coverage clarity | Passed | Orchestrator | Orchestrator | Reworked the playground into a flatter, linear operator layout with a shadcn-style sidebar, removed the boxy card treatment from the content area, added explicit session-on-disk vs indexed-search coverage messaging plus a `Reindex existing sessions` proof action, documented the behavior in README, and verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: Stripe-style API docs interaction pattern | Passed | Orchestrator | Orchestrator | Added endpoint rails and sticky "Try It" panes to the sessions and live-feed sections so the playground behaves more like an API reference with an integrated explorer, while keeping the main response surfaces linear and readable; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: nav-only sidebar + import stats section | Passed | Orchestrator | Orchestrator | Simplified the sidebar to navigation only, moved operational status into the main content flow, added a dedicated backfill/import stats section backed by `/api/v1/search/stats` and polling `/api/v1/sync/status`, and verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: full-screen docs shell + right rail simplification | Passed | Orchestrator | Orchestrator | Converted the playground to a true full-screen docs layout with a left navigation sidebar, wider main documentation column, explicit right rails for runnable examples and JSON results, and simpler Stripe-style section content; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Search: index custom titles and project-identifying metadata | Passed | Orchestrator | Orchestrator | Expanded session indexing and search to cover `customTitle`, session ID, project, cwd, and git branch in addition to summary/first prompt/tag, added a store migration that upgrades legacy SQLite schemas and rebuilds the session FTS table in place, and verified with `npm run build`, `npx vitest run tests/unit/store.test.ts tests/unit/search.test.ts`, and `npx vitest run tests/e2e/api-search.test.ts` |
| 2026-04-07 | Search/Sync: full historical backfill + sidechain-aware indexing | Passed | Orchestrator | Orchestrator | Removed the 1000-session full-index cap, changed reindex ordering to oldest→newest, merged raw `subagents/*.jsonl` transcript messages into their parent session index, persisted subagent relationship metadata, fixed the watcher to recurse into nested transcript folders without emitting startup "discover" noise, corrected `/api/v1/sessions` totals by listing the full discovery set before slicing, and added startup full/incremental index sync; verified with `npm run build`, `npx vitest run tests/unit/transcripts.test.ts tests/unit/session-watcher.test.ts tests/unit/store.test.ts tests/e2e/indexer.test.ts`, plus manual in-process verification that a fresh full index imported all 1422 discovered sessions and that `/api/v1/sessions?limit=5&offset=5&project=/Users/zef/Desktop/cc-middleware` returned the correct total (172). `tests/e2e/api-sessions.test.ts` was blocked by the local Claude usage limit before it could launch a fresh session. |
| 2026-04-07 | Search: expose subagent and team lineage in results | Passed | Orchestrator | Orchestrator | Extended stored session relationships with optional team metadata, parsed team hints from sidechain transcripts, enriched `/api/v1/search` responses with lineage summaries (`subagentCount`, team names, teammate names, relationship details), used current team configs to fill missing team labels at response time, surfaced the lineage badges in the playground search UI, and verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/unit/store.test.ts tests/unit/search.test.ts tests/unit/transcripts.test.ts tests/e2e/api-search.test.ts` |
| 2026-04-07 | Playground/Search: session metadata and indexed scope filters | Passed | Orchestrator | Orchestrator | Added indexed search filters for `all`, `standalone`, `subagent`, and `team` scopes plus optional team-name filtering, expanded the playground session listings with IDs, titles, branches, cwd, and prompt metadata, added a bootstrap inventory summary for team configs/tasks and runtime MCP/skills/plugins, and verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/unit/search.test.ts tests/e2e/api-search.test.ts tests/unit/store.test.ts tests/unit/transcripts.test.ts` |
| 2026-04-07 | Sessions: merged catalog + directory grouping | Passed | Orchestrator | Orchestrator | Added a merged session catalog that overlays raw filesystem discovery with indexed lineage/message metadata, exposed lineage/team filters on `GET /api/v1/sessions`, added `GET /api/v1/sessions/directories` for exact-cwd grouping, refreshed the playground with grouped directory views plus websocket-driven catalog refreshes, and verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/unit/session-catalog.test.ts tests/e2e/api-session-catalog.test.ts tests/unit/search.test.ts tests/e2e/api-search.test.ts` |
| 2026-04-07 | Playground: tree-shaped session explorer with collapsible subagents | Passed | Orchestrator | Orchestrator | Reworked the sessions area into a directory-anchored explorer where parent sessions use a shared metadata grid and subagent lineage is rendered as collapsible child rows under each parent, preserving the linear/docs-like visual style; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: compact Linear-style session row cleanup | Passed | Orchestrator | Orchestrator | Tightened the session explorer rows so titles carry muted session IDs inline, projects sit in a small badge beneath the title, preview copy is removed, and parent/subagent metadata uses a denser uniform grid with lower visual noise; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Search/Sessions: registered metadata schema + metadata-aware search | Passed | Orchestrator | Orchestrator | Added first-class session metadata definitions and values in the SQLite store, exposed metadata CRUD routes under `/api/v1/sessions/metadata*`, attached metadata to `/api/v1/search`, `/api/v1/sessions`, and `/api/v1/sessions/directories`, added searchable/filterable metadata support to session search, and verified with `npm run build` plus `npx vitest run tests/unit/store.test.ts tests/unit/search.test.ts tests/e2e/api-search.test.ts tests/e2e/api-session-catalog.test.ts` |
| 2026-04-07 | Playground: expandable docs nav + metadata/examples lab | Passed | Orchestrator | Orchestrator | Reworked the playground sidebar into expandable documentation-style sections with subsection anchors, added example action lists across sessions/imports/live feed/teams/runtime, introduced a session metadata schema table plus a live metadata lab backed by the new API routes, and verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/unit/store.test.ts tests/unit/search.test.ts tests/e2e/api-search.test.ts tests/e2e/api-session-catalog.test.ts` |
| 2026-04-07 | Playground: multi-page shell + compact overview stats refactor | Passed | Orchestrator | Orchestrator | Split the playground from one long document into page-based views with hash-routed sidebar navigation, extracted shared types/components/page modules out of the monolithic `playground/src/app.tsx`, condensed the overview into compact stats tiles, and verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: merged overview stat strip spacing cleanup | Passed | Orchestrator | Orchestrator | Replaced the overview's separate stat tiles with a single spanning stat surface that uses internal dividers, reduced bottom padding in each stat cell so the strip sits tighter vertically, and verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: global compact stat strip standardization | Passed | Orchestrator | Orchestrator | Tightened the shared stat strip typography and spacing, converted the imports and runtime pages from row-based summaries to the same compact spanning stat surface used on overview, and verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: right-rail operations TOC + inline API examples | Passed | Orchestrator | Orchestrator | Replaced the page-level endpoint strips with a documentation-style right rail that lists operations and section anchors, moved the interactive "try the API" panes into the main content next to their relevant sections, aligned the left sidebar anchors to the new section map, and verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: compact nav + inline top control bars | Passed | Orchestrator | Orchestrator | Slimmed the left navigation width, removed the page-level right rails entirely, replaced large example lists with compact dropdown-driven control bars at the top of each page, and reflowed sessions/imports/live feed/runtime/agents into single-workspace content layouts; verified with `npm run build`, `npx tsc --noEmit`, and `npm run playground:build` |
| 2026-04-07 | Playground: endpoint rail return with inline controls preserved | Passed | Orchestrator | Orchestrator | Restored a lightweight sticky right rail for endpoints and in-page anchors, kept the compact inline toolbars in the main content, and refactored the page shell around a shared `PageBodyWithRail` component so the docs-style layout stays consistent across overview, sessions, imports, live feed, agents, and runtime; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: move example runners into the right rail | Passed | Orchestrator | Orchestrator | Promoted the page quick examples into the shared right sidebar beneath the endpoint list, kept endpoint links at the top of the rail, removed duplicated inline example dropdowns from page content, and left the actual interactive controls in the main column; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: tighter left nav and earlier right-rail breakpoint | Passed | Orchestrator | Orchestrator | Reduced the left sidebar shell by about 30px at desktop sizes and lowered the right utility rail visibility threshold from the old `xl` cutoff to a custom `1200px` minimum width so the endpoint/examples rail stays available on slightly narrower layouts; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: narrower center-column threshold for right rail | Passed | Orchestrator | Orchestrator | Lowered the shared `PageBodyWithRail` breakpoint from `1200px` to `1120px` and tightened the intermediate column gap so the center content can shrink further before the right endpoint/examples rail hides; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: wider right utility rail | Passed | Orchestrator | Orchestrator | Increased the shared right rail width from `220px` to `252px` in `PageBodyWithRail` so the endpoint and example stacks have more room without reworking individual pages; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: runtime validation surface for tools and config inventory | Passed | Orchestrator | Orchestrator | Expanded the runtime page to present actual runtime tools, commands, skills, plugins, MCP servers, agents, and models from `/api/v1/config/runtime`, compare them against discovered skills/commands/agents/plugins/MCP from the middleware config endpoints, and expose richer raw payload previews for validation; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: second right-rail width increase | Passed | Orchestrator | Orchestrator | Increased the shared right utility rail again from `252px` to `288px` so the endpoint list and example controls have more breathing room across all pages; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: split runtime tools and commands into compact table sections | Passed | Orchestrator | Orchestrator | Reworked the runtime page so runtime tools and supported commands are separate sections instead of a side-by-side list block, added a reusable compact table component for denser API-style presentation, and updated the runtime page nav anchors accordingly; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: dedicated runtime tool and command pages | Passed | Orchestrator | Orchestrator | Split runtime tools and supported commands into their own routed pages, extended sidebar child navigation so grouped runtime links can target dedicated pages, and simplified the runtime overview page to focus on the remaining comparison surfaces; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground: compact runtime tables with hover preview cards | Passed | Orchestrator | Orchestrator | Converted the runtime overview plus runtime tools/commands pages from verbose list rows into compact badge-driven tables, moved detailed descriptions and paths into animated hover preview cards, and standardized the runtime validation surfaces around terse reference tables; verified with `npm run build` and `npm run playground:build` |
| 2026-04-07 | Playground/Config: searchable runtime resource pages + floating hover previews | Passed | Orchestrator | Orchestrator | Removed the leftover explanatory top bars from the teams/runtime validation pages, added backend `q=` filtering for config inventories plus dedicated runtime subresource routes (`/api/v1/config/runtime/{tools,commands,skills,plugins,mcp,agents,models}`), split the runtime area into dedicated searchable skills/plugins/MCP/agents/models pages, moved table detail into true floating hover previews that do not consume table space, and verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/e2e/api-config.test.ts` |
| 2026-04-08 | Playground: table control row alignment cleanup | Passed | Orchestrator | Orchestrator | Updated the shared compact data table header so search inputs sit on the left above the table and count/meta badges align on the right, applying the tighter reference-table layout consistently across the runtime inventory pages; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Playground: loading overlay for searchable tables | Passed | Orchestrator | Orchestrator | Changed the shared compact data table so runtime/resource queries keep the search row, count badges, and table frame visible while loading, with a centered spinner overlay instead of collapsing into a separate loading state block; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Playground: denser sidebar navigation styling | Passed | Orchestrator | Orchestrator | Tightened the left sidebar by reducing top-level item font size, padding, gaps, and header spacing, and replaced the active item's left-border treatment with a cleaner filled background state so the nav reads more like a compact Linear-style list; verified with `npm run playground:build` |
| 2026-04-08 | Playground: interactive runtime previews + resource detail drawers | Passed | Orchestrator | Orchestrator | Upgraded the shared runtime/resource tables so hover previews render as true floating popovers instead of non-interactive overlays, added click-through detail drawers for every runtime inventory row, and refined the skills page preview to show truncated descriptions, copyable truncated paths, and no badge clutter while keeping the full description in the drawer; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Playground: normalize passive inventories to compact table pattern | Passed | Orchestrator | Orchestrator | Converted the sessions `Directory groups` surface, the runtime overview's resource-page index, and the teams/agents inventory sections to the shared compact table pattern with hover previews and drawers, leaving only the session explorer as a purpose-built hierarchical tree; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Sessions metadata: schema table, modal editor, and lifecycle management | Passed | Orchestrator | Orchestrator | Added session metadata definition deletion plus usage counts in the store/API, moved metadata filtering into the sessions page toolbar, exposed metadata summaries in the directory groups section, replaced the old metadata lab with a compact schema table and modal editor, and added a selected-session metadata table plus value-application controls; verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/unit/store.test.ts tests/e2e/api-session-catalog.test.ts` |
| 2026-04-08 | Sessions explorer: render per-session metadata as inline tables | Passed | Orchestrator | Orchestrator | Replaced the old prose metadata summary inside session explorer rows with a compact inline metadata table so listed field/value pairs match the rest of the playground’s table language while preserving the parent/subagent tree layout; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Metadata: generic resource metadata API + playground workspaces | Passed | Orchestrator | Orchestrator | Added a generic resource metadata layer in the SQLite store and API (`/api/v1/metadata/definitions/:resourceType` and `/api/v1/metadata/values/:resourceType/:resourceId`) while keeping the existing session metadata APIs working, then introduced reusable metadata workspaces across the runtime tools/commands/skills/plugins/MCP/agents/models pages plus the teams/agents page so non-session inventories can register fields and manage values through the same table-and-modal pattern; verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/unit/store.test.ts tests/e2e/api-session-catalog.test.ts` |
| 2026-04-08 | Tasks: aggregated task registry API + dedicated playground page | Passed | Orchestrator | Orchestrator | Added an aggregated `GET /api/v1/tasks` surface over Claude team task files, enriched per-team task responses with stable `resourceId`, team identity, and source paths, promoted tasks into a dedicated Tasks page in the playground with searchable aggregate and per-team tables plus task metadata support, and also surfaced the selected team's tasks directly on the Agents and teams page; verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/e2e/api-tasks.test.ts tests/e2e/api-agents.test.ts` |
| 2026-04-08 | Playground: session-first sidebar hierarchy with utilities at the bottom | Passed | Orchestrator | Orchestrator | Reorganized the left navigation so teams, tasks, and agents now sit under the Sessions parent to reflect how those objects relate to session activity, moved operational surfaces like Live feed and Imports into a visually separated utility block at the bottom of the sidebar, and verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Playground/Config: separate effective runtime from declarative configuration | Passed | Orchestrator | Orchestrator | Added a first-class Configuration section with dedicated pages for overview, settings/projects, plugins/marketplaces, discovered skills, discovered commands, discovered agent files, MCP definitions, and memory/guidance content; refactored runtime skills/commands/plugins/MCP/agents pages to be runtime-only; added middleware-backed `q=` filtering for tracked projects, available plugins, marketplaces, rules, memory files/projects, and CLAUDE.md discovery routes; and verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/e2e/api-config.test.ts` |
| 2026-04-08 | Playground: resource-first IA with dedicated Teams and Agents pages | Passed | Orchestrator | Orchestrator | Reworked the playground sidebar around first-class middleware resources by making the `sessions` nav group a resource catalog (`Sessions`, `Teams`, `Agents`, `Tasks`, `Tools`), split the old combined agents/teams surface into dedicated searchable Teams and Agents pages, and wired those pages directly to `/api/v1/teams`, `/api/v1/teams/:name`, `/api/v1/teams/:name/tasks`, `/api/v1/agents`, and `/api/v1/agents/:name`; verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/e2e/api-agents.test.ts tests/e2e/api-tasks.test.ts tests/e2e/api-session-catalog.test.ts` |
| 2026-04-08 | Metadata: searchable resource metadata tables with per-resource page ownership | Passed | Orchestrator | Orchestrator | Added backend `q=` filtering to generic metadata routes plus team/agent listing routes, updated the shared metadata workspace so both metadata tables have search like the other reference tables, moved the `Add field` action into the metadata-fields table header on the right, and ensured each first-class resource page now owns its own metadata workspace instead of sharing a combined teams/agents metadata lab; verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/e2e/api-agents.test.ts tests/e2e/api-tasks.test.ts tests/e2e/api-session-catalog.test.ts` |
| 2026-04-08 | Phase 13 analytics planning and documentation | Passed | Orchestrator | Orchestrator | Added analytics architecture, research findings, and a dependency-aware implementation phase with parallel workstreams, task ownership, and planned API/UI surfaces to guide transcript-first analytics development. |
| 2026-04-08 | Task 13.1 | Passed | McClintock | Nietzsche | Added DuckDB analytics warehouse creation, schema migration support, raw table scaffolding, and unit coverage for idempotent initialization via `src/analytics/db.ts`, `src/analytics/schema/0001_init.sql`, and `tests/unit/analytics-db.test.ts`; verified with `npm run build` and `npx vitest run tests/unit/analytics-db.test.ts`. |
| 2026-04-08 | Task 13.2 | Passed | Orchestrator | Nietzsche | Added transcript discovery, parsing, and DuckDB backfill import for root and subagent JSONL history, including redacted fixtures and idempotent raw-event ingestion tests; verified with `npm run build` and `npx vitest run tests/unit/analytics-backfill.test.ts`. |
| 2026-04-08 | Task 13.3 | Passed | Euclid | Nietzsche | Added deterministic English keyword taxonomy and matcher coverage for frustration, cursing, insults, aggression, and urgency, with targeted unit tests for false-positive guardrails and category assignment; verified with `npm run build` and `npx vitest run tests/unit/analytics-keywords.test.ts`. |
| 2026-04-08 | Task 13.4 | Passed | Singer + Erdos + Orchestrator | Nietzsche | Added raw live SDK/hook/permission capture helpers, REST/WebSocket analytics launch options, broader plugin hook coverage, a DuckDB-backed live sink wired through `main.ts`, and route-level `canUseTool` plumbing so middleware-launched sessions now persist runtime analytics through the shared permission manager; verified with `npm run build` and `npx vitest run tests/unit/live-analytics.test.ts tests/unit/live-analytics-db.test.ts tests/unit/permission-handler.test.ts tests/e2e/api-analytics.test.ts`. Residual risk: extremely early permission checks can still see a blank `session_id` before the SDK reveals the real one. |
| 2026-04-08 | Task 13.5 | Passed | Orchestrator | Nietzsche | Added versioned pricing helpers plus a refreshable derived warehouse layer under `src/analytics/pricing.ts`, `src/analytics/derive/`, and `src/analytics/schema/0002_derived.sql`, producing fact tables for interactions, requests, tool calls, errors, subagent runs, compactions, keyword mentions, permission decisions, and hourly/daily rollups; verified with `npm run build` and `npx vitest run tests/unit/analytics-derive.test.ts tests/unit/analytics-db.test.ts tests/unit/analytics-backfill.test.ts tests/unit/analytics-keywords.test.ts tests/unit/live-analytics.test.ts tests/unit/live-analytics-db.test.ts tests/unit/permission-handler.test.ts tests/e2e/api-analytics.test.ts`. |
| 2026-04-08 | Task 13.6 | Passed | Locke + Orchestrator | Orchestrator | Added the analytics API surface in `src/api/routes/analytics.ts` with `/api/v1/analytics/status`, `/overview`, `/timeseries`, `/traces`, `/traces/:id`, `/sessions/:id`, `/facets`, and `/backfill`, wired it through `src/api/server.ts`, refreshed shared analytics response types, and shifted the API onto the derived warehouse tables where available; verified with `npm run build` and `npx vitest run tests/e2e/api-analytics.test.ts tests/unit/analytics-derive.test.ts tests/unit/analytics-db.test.ts tests/unit/analytics-backfill.test.ts tests/unit/analytics-keywords.test.ts tests/unit/live-analytics.test.ts tests/unit/live-analytics-db.test.ts tests/unit/permission-handler.test.ts`. |
| 2026-04-08 | Task 13.7 | Passed | Orchestrator | Newton | Added the analytics playground surface in `playground/src/pages/analytics-page.tsx` with navigation wiring in `playground/src/lib/playground.ts` and `playground/src/app.tsx`, plus chart/facet/trace/drilldown components in `playground/src/components/analytics-timeseries.tsx`, `playground/src/components/analytics-facets.tsx`, `playground/src/components/analytics-trace-table.tsx`, and `playground/src/components/analytics-drawer.tsx`; the page supports range presets, metric overlays, trace/session drilldown, and backfill refresh, and verified cleanly with `npm run build`, `npx vitest run tests/e2e/api-analytics.test.ts tests/unit/analytics-derive.test.ts tests/unit/analytics-db.test.ts tests/unit/analytics-backfill.test.ts tests/unit/analytics-keywords.test.ts tests/unit/live-analytics.test.ts tests/unit/live-analytics-db.test.ts tests/unit/permission-handler.test.ts`, and `npm run playground:build`. |
| 2026-04-08 | Task 13.8 | Passed | Orchestrator | Orchestrator | Added an optional OTel/telemetry enrichment path under `src/analytics/otel/` that discovers local telemetry files, parses Claude telemetry JSONL plus span-shaped records, redacts sensitive nested prompt/tool payloads by default, and imports rows into `raw_otel_logs` / `raw_otel_spans` through the analytics backfill route when `includeOtel` is requested; verified with `npm run build` and `npx vitest run tests/unit/analytics-otel.test.ts tests/e2e/api-analytics.test.ts`. |
| 2026-04-08 | Task 13.9 | Passed | Orchestrator | Orchestrator | Added analytics fixtures for optional telemetry enrichment, expanded targeted verification to cover transcript backfill, live capture, and optional telemetry import, updated the analytics API reference to document real `/api/v1/analytics/*` routes and backfill request semantics, and added metric-semantics plus operator-guide sections to `docs/architecture/analytics-system.md`; verified with `npm run build`, `npx vitest run tests/unit/analytics-db.test.ts tests/unit/analytics-backfill.test.ts tests/unit/analytics-keywords.test.ts tests/unit/live-analytics.test.ts tests/unit/live-analytics-db.test.ts tests/unit/analytics-derive.test.ts tests/unit/analytics-otel.test.ts tests/unit/permission-handler.test.ts tests/e2e/api-analytics.test.ts`, and `npm run playground:build`. |
| 2026-04-08 | Session-scoped resource filters + session-first naming cleanup | Passed | Orchestrator | Orchestrator | Added `sessionId` / `sessionIds` filtering to `/api/v1/teams`, `/api/v1/tasks`, `/api/v1/teams/:name/tasks`, and `/api/v1/agents` by resolving indexed session relationship/team lineage data through the session store; updated the playground so Teams, Agents, and Tasks expose explicit session-scope controls; and renamed the resource hierarchy back to a session-first model where the sidebar section is `Sessions` and the primary sessions page is labeled `Agent sessions`; verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/e2e/api-agents.test.ts tests/e2e/api-tasks.test.ts tests/e2e/api-session-catalog.test.ts` |
| 2026-04-08 | Sessions page: default to latest sessions table instead of empty search state | Passed | Orchestrator | Orchestrator | Removed the hardcoded default session query so the sessions page no longer boots into an empty search result, changed the top session surface from the old grouped tree into a compact latest-sessions/search-results table that matches the rest of the playground, moved the search input into that table header, and increased the default latest-session fetch size to 24; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Playground tables: preserve table shell for empty resource inventories | Passed | Orchestrator | Orchestrator | Updated the shared `CompactDataTable` so empty or zero-result inventories keep their headers, search controls, meta badges, and table chrome instead of collapsing to the old placeholder block; empty and error states now render inside the table body, which fixes Teams, Tasks, Agents, and other resource pages that were still visually falling back to the old list-empty pattern; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Metadata workspace: move actions into table headers and row menus | Passed | Orchestrator | Orchestrator | Reworked the shared resource metadata workspace so the old standalone `Metadata actions` section is gone across all resource pages; field creation now lives in the metadata-fields table header, field/value actions are handled through 3-dot row menus with dropdown actions, destructive removals require confirmation input in a modal, value editing happens in a dedicated modal, and the remaining context UI is reduced to a slim inventory/resource selector row; also tightened the metadata hook with explicit resource-targeted save/delete helpers and fixed unrelated analytics route typing so `npm run build` stays green; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Metadata workspace: replace selected-resource strip with table filters | Passed | Orchestrator | Orchestrator | Removed the persistent resource selector/badge strip from the shared metadata workspace, added an aggregate `GET /api/v1/metadata/values/:resourceType` route so metadata values can list recent entries by default, moved inventory/resource narrowing into compact table-header filters, and kept resource targeting inside the value modal only when an action actually needs it; verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/e2e/api-session-catalog.test.ts` |
| 2026-04-08 | Playground sidebar: muted all-caps section labels with background active states | Passed | Orchestrator | Orchestrator | Tightened the shared left navigation so top-level items now read as smaller muted all-caps section labels, child routes carry the darker navigable text, and both levels use a filled background to indicate selection instead of relying on border treatments; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Playground sidebar: leaf-only active states and denser child rows | Passed | Orchestrator | Orchestrator | Reduced top-level section labels by another step, lightened child route weight, removed active-background treatment from expandable section labels so only actual child destinations read as selected, switched child hover to a tinted version of the active fill instead of white, and reduced child vertical padding for a denser sidebar rhythm; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Playground sidebar: tighter child indent and shorter one-line labels | Passed | Orchestrator | Orchestrator | Reduced child-nav left indent and horizontal padding, then shortened several sidebar child labels in the shared navigation model (`Sessions`, `Tools`, `Commands`, `MCP`, `Payload`, `Settings`, `Plugins`, `Memory`, `Status`) so they hold a one-line rhythm within the narrower rail without increasing sidebar width; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Playground sidebar: top-level items now disclose instead of navigate | Passed | Orchestrator | Orchestrator | Changed the shared sidebar so clicking a top-level section label now expands or collapses that group rather than navigating; the actual route navigation remains on the child items, and the disclosure control now exposes `aria-expanded` and `aria-controls` for clearer semantics; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Compact table previews: keep hover popovers on the right side | Passed | Orchestrator | Orchestrator | Updated the shared compact-table preview positioning logic so hover popovers clamp within the viewport on the right side instead of flipping to the left side of the table when space gets tight, which fixes the awkward preview placement seen on skills and other runtime/config tables; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Playground sidebar: collapsed by default and persisted locally | Passed | Orchestrator | Orchestrator | Changed the app shell so all top-level sidebar groups begin collapsed by default, restored expand/collapse state from `localStorage` on boot, and persisted every toggle back to local storage so the nav comes back in the same shape on refresh; also tightened an unrelated OTel importer type signature so the full TypeScript build stays green again; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Overview page: remove top-of-page notes and move refresh into the rail | Passed | Orchestrator | Orchestrator | Removed the extra `Session behavior` and `Layout model` note block from the overview content, dropped the old workspace toolbar so `Status` is now the first content section under the page intro, added an optional right-rail workspace utility panel for the overview page with `Refresh workspace` plus the local API/UI endpoints, and also repaired the OTel importer/barrel contract so the full TypeScript build remained green; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Detail drawers: copyable ids/paths, wider layout, and wrapped session payload previews | Passed | Orchestrator | Orchestrator | Updated the shared compact-table drawer so string-valued meta rows with labels like `id`, `uuid`, `path`, and `directory` automatically render with a copy-to-clipboard affordance, widened the right-side detail drawer, and changed the shared JSON preview to wrap long lines inside a horizontally safe container so session payload snippets no longer overflow off the right edge or trap scrolling near the bottom; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Code blocks: native two-axis scrolling inside JSON/detail previews | Passed | Orchestrator | Orchestrator | Reworked the shared `JsonPreview` code block surface to use a plain `overflow-auto` container with overscroll containment instead of the custom scroll area wrapper, so overflowed payloads can be scrolled directly inside the code block in both axes; JSON/code payloads now preserve preformatted width when present, while empty-message states still wrap naturally; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Drawer meta rows: copy on row click and compact session titles in the sessions table | Passed | Orchestrator | Orchestrator | Updated the shared compact-table meta renderer so ID/path-style preview and drawer rows are fully clickable copy targets with an inline copy state indicator, which also gives the session hover preview a copy affordance on `Session id`; additionally constrained the session-title cell width in the main sessions table so long names stay truncated instead of stretching across the entire table; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Playground theme system: Mintlify-aligned light/dark switcher in the sidebar | Passed | Orchestrator | Orchestrator | Added a persisted light/dark theme selector to the bottom of the left sidebar, applied the selected theme to the document root with local storage restore, introduced shared playground color tokens using the Mintlify docs accent colors (`#6C47FF`, `#8B6CFF`, `#5230CC`), and updated the shared input/button/select/textarea/badge primitives plus dark-mode utility overrides so the shell, tables, drawers, and controls restyle coherently without page-by-page rewrites; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Shared stat strips: convert status/bootstrap/import summary surfaces to theme tokens | Passed | Orchestrator | Orchestrator | Reworked the shared `CompactStatGrid` and stat-strip CSS so status and bootstrap summary rows no longer rely on light-only `slate` borders and pale gradients; the shared stat surfaces now use tokenized container, text, and tone backgrounds in both light and dark themes, which immediately fixes overview, runtime, imports, analytics, and config summary strips together; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Shared stat strips: darker base, subtler tone overlays, and non-white borders | Passed | Orchestrator | Orchestrator | Tightened the stat-strip theme tokens again so the summary surfaces sit on a darker shared base, use much subtler tone washes layered over that base, and render with an explicit tinted border instead of bright white edges; this keeps status/bootstrap/import/runtime summaries closer to the rest of the dark shell and fixes the overly bright card look in dark mode; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Tables: explicit eye-triggered previews and no redundant indexed badges | Passed | Orchestrator | Orchestrator | Changed the shared compact table so preview cards no longer appear from hovering an entire row; tables now render a muted eye icon in the final column and only show the preview after a 400ms hover/focus delay on that control, while row clicks still open the full detail drawer. Also removed the positive `indexed` badges from the session and directory tables so only genuinely actionable pending-index states remain visible; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Session detail workspace: source-of-truth transcript route plus virtualized inspector UI | Passed | Orchestrator | Newton | Added a raw-transcript-backed `GET /api/v1/sessions/:id/detail` route and builder for root or subagent sessions, including transcript turns plus derived files/tools/errors/skills/config/subagent inspector data; added a dedicated hidden `session-detail` playground route with row navigation from the sessions table, a virtualized chat/worklog transcript, and a right-hand inspector sidebar; integrated the nested backend payload with a playground normalization layer so the new UI renders the actual API contract instead of the older flat assumptions. Verified with `npm run build`, `npm run playground:build`, `npx vitest run tests/unit/session-detail.test.ts tests/e2e/api-session-detail.test.ts`, and a real local `buildSessionDetail()` sanity check against an existing Claude session. |
| 2026-04-08 | Session detail redesign: dock chat in the right rail and simplify inspector chrome | Passed | Orchestrator | Orchestrator | Reworked the session detail page so the main pane is now a calmer overview/work surface and the right rail is a tabbed utility panel with `Chat log`, `Activity`, `Context`, and `Subagents`; moved the virtualized transcript into that rail, removed the heavy transcript cards/icon badges, and simplified the inspector rows so they read as compact operational lists instead of a stack of note-like cards. Verified with `npm run build` and `npm run playground:build`. |
| 2026-04-08 | Session detail theme polish: token-backed typography plus animated sidebar theme switch | Passed | Orchestrator | Orchestrator | Replaced the remaining hard-coded session-detail text/divider colors with shared theme-token classes so light mode no longer leaves low-contrast copy behind, updated the shared tabs primitive to respect theme variables, and replaced the sidebar theme `<select>` with a compact animated light/dark toggle that swaps sun/moon icons as it slides. Verified with `npm run build` and `npm run playground:build`. |
| 2026-04-08 | Session detail transcript: preserve tool structure and render specialized activity blocks | Passed | Orchestrator | Newton | Extended the playground session-detail model so transcript items preserve structured tool/task payloads via transcript `variant`, `fields`, `codeBlocks`, and `todoItems`, rewired `playground/src/lib/session-detail.ts` to render `Write`, `Edit`, `MultiEdit`, `TodoWrite`, `Bash`, search/read tools, and tool results into specialized transcript items, rebuilt `playground/src/components/session-detail-transcript.tsx` around turn-based rendering with user prompts, assistant copy, task lists, expandable code viewers, and structured tool/result panels, and threaded transcript turns through the sidebar/page while adding transcript-specific tokenized styles in `playground/src/index.css`; verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/unit/session-detail-normalize.test.ts tests/unit/session-detail.test.ts tests/e2e/api-session-detail.test.ts`. |
| 2026-04-08 | Session detail transcript: detect Claude tag blobs and render them through expandable markdown | Passed | Orchestrator | Newton | Added `react-markdown` plus a transcript-markup normalizer in `playground/src/lib/transcript-markdown.ts` so Claude-specific tagged text like `<command-name>`, `<local-command-stdout>`, `<task-notification>`, and `<tool_use_error>` is converted into safe markdown before rendering; introduced reusable transcript markdown/code viewer components in `playground/src/components/session-detail-markdown.tsx` and `playground/src/components/session-detail-code-viewer.tsx`; and updated `playground/src/components/session-detail-transcript.tsx` plus `playground/src/index.css` so user, assistant, system, and tool text can render in expandable markdown surfaces instead of plain text dumps. Verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/unit/transcript-markdown.test.ts tests/unit/session-detail-normalize.test.ts tests/unit/session-detail.test.ts tests/e2e/api-session-detail.test.ts`. |
| 2026-04-08 | Session detail transcript: derive turn headers from normalized message summaries | Passed | Orchestrator | Orchestrator | Added `playground/src/lib/session-detail-summary.ts` so turn headers summarize normalized transcript items instead of echoing raw backend `turn.summary`, which fixes examples where a turn preview previously showed raw `<task-notification>` payloads even though the body renderer was already polished; verified with `npm run build`, `npm run playground:build`, and `npx vitest run tests/unit/session-detail-summary.test.ts tests/unit/transcript-markdown.test.ts tests/unit/session-detail-normalize.test.ts tests/unit/session-detail.test.ts tests/e2e/api-session-detail.test.ts`. |
| 2026-04-08 | Playground sidebar: move Analytics below Live feed in the utility group | Passed | Orchestrator | Orchestrator | Reordered the shared `navigationSections` model so Analytics now appears after the utility-group separator and directly below Live feed in the left sidebar, without changing route wiring or separator rendering; verified with `npm run build` and `npm run playground:build` |
| 2026-04-08 | Analytics: restore real time bucketing and range filtering for the trends view | Passed | Orchestrator | Orchestrator | Added a shared DuckDB timestamp coercion helper in `src/analytics/timestamps.ts`, wired both `src/analytics/derive/refresh.ts` and `src/api/routes/analytics.ts` to use it, rebuilt the derived warehouse so historical timestamps no longer collapse to `Date.now()`, and added regression coverage in `tests/unit/analytics-timestamps.test.ts` plus a multi-day range test in `tests/e2e/api-analytics.test.ts`; verified with `npm run build`, `npx vitest run tests/unit/analytics-timestamps.test.ts tests/unit/analytics-derive.test.ts tests/e2e/api-analytics.test.ts`, and a live browser screenshot pass against the analytics page. |
| 2026-04-08 | Analytics trends: separate low-volume signals from token scale | Passed | Orchestrator | Orchestrator | Updated `playground/src/components/analytics-timeseries.tsx` so errors and keyword hits render on a dedicated left signal axis, token overlays use their own secondary scale, estimated cost stays on its own right axis, and sparse signal series now draw visible dots; verified with `npm run build`, `npm run playground:build`, direct `/api/v1/analytics/timeseries` checks, and a fresh browser screenshot of the analytics page. |
| 2026-04-08 | Analytics dashboard: separate the full-width dashboard from playground docs content and add a left filter rail | Passed | Orchestrator | Orchestrator | Rebuilt `playground/src/pages/analytics-page.tsx` as a dedicated full-width dashboard page with a sticky left filter sidebar for range, facet, search, and session-scope controls; extended `src/api/routes/analytics.ts` so overview, timeseries, traces, and facets all accept the shared analytics facet filters; added filter-aware API coverage in `tests/e2e/api-analytics.test.ts`; and verified with `npm run build`, `npm run playground:build`, `npx vitest run tests/e2e/api-analytics.test.ts`, direct `/api/v1/analytics/*` filter checks, and a live browser screenshot pass against `http://127.0.0.1:4173/#/analytics`. |
| 2026-04-08 | Analytics trends: replace card-heavy chart chrome with an open plotting surface | Passed | Orchestrator | Orchestrator | Reworked `playground/src/components/analytics-timeseries.tsx` and chart-specific theme styles in `playground/src/index.css` so the trends area uses a custom legend, quieter axes/grid, layered gradient fills, atmospheric color glows, and an open chart frame instead of the previous generic bordered card plus default Recharts legend; verified with `npm run build`, `npm run playground:build`, and a fresh browser screenshot pass against `http://127.0.0.1:4173/#/analytics`. |
| 2026-04-08 | Analytics dashboard: add per-tool reliability table and clearer session-type controls | Passed | Orchestrator | Orchestrator | Wired the existing filtered `/api/v1/analytics/tool-performance` dataset into the analytics dashboard, added a `Tool reliability` table directly below Trends in `playground/src/components/analytics-tool-table.tsx`, surfaced quick session-type chips in both the main summary area and the tool table controls via `playground/src/pages/analytics-page.tsx`, and added a dedicated `Tools` analytics section anchor in `playground/src/lib/playground.ts`; verified with `npm run build`, `npm run playground:build`, `npx vitest run tests/e2e/api-analytics.test.ts`, and a fresh live browser screenshot pass against `http://127.0.0.1:4173/#/analytics`. |
| 2026-04-08 | Analytics tables: dark-mode polish and tool failure drilldown drawer | Passed | Orchestrator | Orchestrator | Reworked the analytics table surfaces around shared theme classes in `playground/src/index.css`, removed the tool table's progress-bar and redundant failure/occurrence chrome in `playground/src/components/analytics-tool-table.tsx`, added clickable per-tool drilldown through `playground/src/components/analytics-tool-drawer.tsx` and `GET /api/v1/analytics/tool-performance/:name`, rethemed the shared modal shell in `playground/src/components/playground-ui.tsx`, and verified with `npm run build`, `npm run playground:build`, `npx vitest run tests/e2e/api-analytics.test.ts`, and a live browser check of the analytics page. |
| 2026-04-08 | Analytics dashboard: remove the redundant keyword/warehouse summary block | Passed | Orchestrator | Orchestrator | Removed the mid-page `Keyword breakdown` and `Warehouse status` cards from `playground/src/pages/analytics-page.tsx` so the analytics dashboard moves more directly from summary stats into Trends, then Tool reliability and Traces; verified with `npm run build`. |
| 2026-04-08 | Analytics filter rail: replace note-like cards with a denser facet explorer | Passed | Orchestrator | Averroes | Reworked the analytics left rail in `playground/src/pages/analytics-page.tsx` and `playground/src/index.css` so it no longer renders as stacked rounded cards with explanatory filler; the rail now uses divider-separated sections, tighter uppercase headings, a searchable Tools facet, quieter workspace/status treatment, and a continuous explorer-style layout; verified with `npm run build` and `npm run playground:build`. |
| 2026-04-08 | Analytics chart tooltip: compact surface polish | Passed | Orchestrator | Orchestrator | Tightened the analytics timeseries tooltip in `playground/src/components/analytics-timeseries.tsx` and `playground/src/index.css` so it uses a smaller typographic scale, a deliberate translucent surface, stronger contrast in both themes, and compact value rows instead of oversized default Recharts chrome; verified with `npm run build` and `npm run playground:build`. |
| 2026-04-08 | Analytics tables: sortable headers and trace search aligned with the table explorer pattern | Passed | Orchestrator | Goodall | Added sortable headers to both `playground/src/components/analytics-tool-table.tsx` and `playground/src/components/analytics-trace-table.tsx`, moved trace search out of the left filter rail and into the trace table control row through `playground/src/pages/analytics-page.tsx`, added shared sort affordance styling in `playground/src/index.css`, and verified with `npm run build` and `npm run playground:build`. |
| 2026-04-08 | Sessions page: remove redundant search coverage helper block | Passed | Orchestrator | Parfit | Removed the two `InlineState` helper messages beneath the sessions search toolbar in `playground/src/pages/sessions-page.tsx` so the explorer table follows the controls directly; verified with `npm run build` and `npm run playground:build`. |
| 2026-04-08 | Playground: restore page load after sidebar theme-switcher regression | Passed | Orchestrator | Newton | Fixed a runtime-only import bug in `playground/src/app.tsx` by importing `MoonStar` and `SunMedium` from `lucide-react` instead of `react`, added a pre-paint theme bootstrap in `playground/index.html` so saved light/dark mode applies before React mounts, and re-verified the shell with `npm run build`, `npm run playground:build`, a live smoke test through `http://127.0.0.1:4173/`, `/api/v1/status`, and `/api/v1/sessions?limit=1`, plus a separate verifier pass confirming no blocking page-load or theme-application issues remained. |
| 2026-04-08 | Session detail: full-height inspector rail and distinct chat panel | Passed | Orchestrator | Newton | Updated the session detail layout so the right utility rail stretches as a sticky full-height inspector on desktop, taught `SessionDetailTranscript` to fill embedded panel height, and gave the chat log its own themed panel surface instead of blending into the page background; verified with `npm run build` and `npm run playground:build`. |
| 2026-04-08 | Session detail: remove redundant sidebar copy and compress lightweight tool activity | Passed | Orchestrator | Newton | Removed the sidebar’s extra utility-panel heading in `playground/src/components/session-detail-sidebar.tsx`, hid the duplicate transcript header inside the chat tab, and reworked `playground/src/components/session-detail-transcript.tsx` plus `playground/src/index.css` so lightweight log items like `Read`, `Search`, and simple tool results render as single-line status-dot activity rows while heavier write/edit/bash entries use a much more open stacked layout instead of boxed mini-cards; verified with `npm run build` and `npm run playground:build`. |
| 2026-04-08 | Playground: install Agentation feedback overlay and register Codex MCP | Passed | Orchestrator | Newton | Installed `agentation` and `agentation-mcp`, added `agentation:server` and `agentation:doctor` scripts in `package.json`, mounted the Agentation overlay at the root of `playground/src/app.tsx` in dev-only mode with a `VITE_AGENTATION_ENDPOINT` override and `http://localhost:4747` fallback, registered the `agentation` MCP server in `~/.codex/config.toml` via `codex mcp add`, and verified the live server on `http://127.0.0.1:4747/health`; verified with `npm run build`, `npm run playground:build`, `codex mcp list`, and live port/health checks. |
| 2026-04-08 | Session detail: compact linear chat polish for transcript and composer | Passed | Orchestrator | Orchestrator | Reworked the session detail chat tab in `playground/src/components/session-detail-sidebar.tsx`, `playground/src/components/session-detail-transcript.tsx`, and `playground/src/index.css` so the top rail is a slimmer underline nav, the transcript reads as one continuous compact lane with quieter metadata and content-first activity rows, and the bottom composer behaves like a docked chat control instead of a separate form section; verified with `npm run build` and `npm run playground:build`. |
| 2026-04-08 | Session detail: compact read/result transcript rows and drop empty assistant placeholders | Passed | Orchestrator | Orchestrator | Tightened transcript normalization in `playground/src/lib/session-detail.ts`, `playground/src/lib/session-detail-summary.ts`, and `playground/src/components/session-detail-transcript.tsx` so simple `Read` tool events no longer dump raw JSON input, tool-result summaries do not repeat status/tool labels, read-result code viewers inherit the file path, and thinking-only assistant placeholders are omitted before grouping; verified with `npx vitest run tests/unit/session-detail-normalize.test.ts tests/unit/session-detail-summary.test.ts tests/unit/session-chat-turns.test.ts`, `npm run build`, and `npm run playground:build`. |
| 2026-04-08 | Session detail: inline code stays inline and redundant read-path badges are suppressed | Passed | Orchestrator | Orchestrator | Updated `playground/src/components/session-detail-markdown.tsx` to distinguish block code via the `pre` wrapper instead of relying on a missing `inline` prop from `react-markdown`, and updated `playground/src/components/session-detail-transcript.tsx` so file badges are hidden when the same path is already represented by the summary, `filePath`, or a code block path; added render regressions in `tests/unit/session-detail-rendering.test.tsx` and verified with `npx vitest run tests/unit/session-detail-rendering.test.tsx tests/unit/session-detail-normalize.test.ts tests/unit/session-detail-summary.test.ts tests/unit/session-chat-turns.test.ts`, `npm run build`, and `npm run playground:build`. |
| 2026-04-08 | Session detail: merge tool-use and tool-result rows in presentation and widen the right rail | Passed | Orchestrator | Orchestrator | Reworked `playground/src/components/session-detail-transcript.tsx` so tool activities and their matching `tool_result` messages collapse into one rendered block with one headline, one status pill, nested child activity support, and result details in the same unit; widened the session detail right rail in `playground/src/pages/session-detail-page.tsx`, added supporting spacing rules in `playground/src/index.css`, and extended `tests/unit/session-detail-rendering.test.tsx` to verify merged rendering; verified with `npx vitest run tests/unit/session-detail-rendering.test.tsx tests/unit/session-detail-normalize.test.ts tests/unit/session-detail-summary.test.ts tests/unit/session-chat-turns.test.ts`, `npm run build`, and `npm run playground:build`. |
